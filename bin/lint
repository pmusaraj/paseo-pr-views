#!/usr/bin/env node
import { spawnSync } from "node:child_process";
const args = ["--type-aware", "."];
if (process.argv.includes("--fix")) args.unshift("--fix");
const result = spawnSync("node_modules/.bin/oxlint", args, { stdio: "inherit" });
process.exit(result.status ?? 1);
